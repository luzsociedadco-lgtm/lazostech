import AssetLayerRoleSetup from "./AssetLayerRoleSetup";

export const metadata = {
  title: "Roles Asset Layer | LazosTech",
  description: "Asignación segura de roles testnet para el Asset Layer de LazosTech."
};

export default function AssetLayerRolesPage() {
  return <AssetLayerRoleSetup />;
}
