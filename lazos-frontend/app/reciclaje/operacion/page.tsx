import AssetLayerConsole from "./AssetLayerConsole";

export const metadata = {
  title: "Asset Layer | LazosTech",
  description: "Operación empresarial de materiales circulares verificables."
};

export default function AssetLayerOperationPage() {
  return <AssetLayerConsole />;
}
